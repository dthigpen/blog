---
title: David Thigpen's Blog
layout: main
---
<h2>My Cool index template</h2>
List of blog here, newest first
{% assign posts = collections.posts | reverse %}
{% for post in posts limit: 3 %}
  <p>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    <i>(written {{ post.date | date: "%b %d, %Y" }})</i>
  </p>
{% endfor %}